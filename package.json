{
  "name": "redconnect",
  "version": "2.0.0",
  "private": true,
  "description": "Community blood donor and emergency request platform",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node -e \"console.log('RedConnect static assets are ready in public/')\"",
    "check": "node --check server.js && node --check public/app.js && node --check public/database.js && node --check public/blood-network.js"
  },
  "engines": { "node": ">=18" }
}
